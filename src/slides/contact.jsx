import React from "react";
import Page from "../components/Page";

const Contact = ({ title, light, blueLine }) => <Page light={light} blueLine={blueLine} title={title} className="section section--gradient-black" />;

export default Contact;
